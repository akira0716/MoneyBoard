<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MoneyBoard.Views.UncategorizedTransactionsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Uncategorized Transactions">
    <Grid
        Padding="10"
        RowDefinitions="*, Auto"
        RowSpacing="10">

        <CollectionView
            Grid.Row="0"
            ItemsSource="{Binding UncategorizedUsageNames}"
            SelectedItem="{Binding SelectedUsageName}"
            SelectionMode="Single">
            <CollectionView.Header>
                <Label
                    Padding="5"
                    FontAttributes="Bold"
                    Text="Select a usage name to categorize:" />
            </CollectionView.Header>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Frame Margin="5" Padding="10">
                        <Label
                            FontAttributes="Bold"
                            Text="{Binding .}"
                            TextColor="{AppThemeBinding Light=Black,
                                                        Dark=White}" />
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup Name="CommonStates">
                                <VisualState Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="Transparent" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState Name="Selected">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="LightBlue" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <VerticalStackLayout
            Grid.Row="1"
            IsVisible="{Binding IsUsageNameSelected}"
            Spacing="10">
            <Picker
                Title="Select Category"
                ItemDisplayBinding="{Binding Name}"
                ItemsSource="{Binding AvailableCategories}"
                SelectedItem="{Binding SelectedCategory}" />
            <Button Command="{Binding AssignCategoryCommand}" Text="Assign Category and Create Mapping" />
        </VerticalStackLayout>

    </Grid>
</ContentPage>
